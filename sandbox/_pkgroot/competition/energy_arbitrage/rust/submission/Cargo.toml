[package]
name = "submission"
version = "0.1.0"
edition = "2021"

[dependencies]
eval = { path = "../eval" }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = { version = "0.8", default-features = false, features = ["std_rng", "small_rng"] }
